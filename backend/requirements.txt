# ── Django Core ───────────────────────────────────────────────────────────────
Django>=4.2,<5.0
djangorestframework>=3.14
django-cors-headers>=4.3
django-filter>=23.0
django-jazzmin>=2.6            # Modern admin UI (sidebar, dark mode, cards)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9        # PostgreSQL driver (production)
dj-database-url>=2.1        # Parse DATABASE_URL env var

# ── Task Queue ────────────────────────────────────────────────────────────────
celery>=5.3
redis>=5.0

# ── MQTT ──────────────────────────────────────────────────────────────────────
paho-mqtt>=1.6
certifi>=2024.0       # CA bundle for HiveMQ Cloud TLS verification

# ── Production Server ─────────────────────────────────────────────────────────
gunicorn>=21.2

# ── Notifications ─────────────────────────────────────────────────────────────
twilio>=8.0
exponent-server-sdk>=0.2.0

# ── Rate Limiting ─────────────────────────────────────────────────────────────
django-ratelimit>=4.1            # Brute-force protection on login + IoT ingestion

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0
